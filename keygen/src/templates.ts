export const HEART_SVG = ``;
export const SHIELD_SVG = ``;
export const FASCIA_SVG = ``;

export interface TemplateItem {
  name: string;
  svg: string;
  icon: string;
}

export const TEMPLATES: TemplateItem[] = [
  // System templates removed by user request
];
